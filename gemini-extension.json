{
  "name": "kuri-browser-agent",
  "version": "0.6.0",
  "description": "Kuri-powered browser agent extension with MCP bridge.",
  "settings": [
    {
      "name": "Kuri binary path",
      "description": "Path to the kuri binary (defaults to kuri)",
      "envVar": "KURI_PATH",
      "sensitive": false
    },
    {
      "name": "Proxy URL",
      "description": "Default proxy URL, including credentials if required",
      "envVar": "KURI_PROXY",
      "sensitive": true
    },
    {
      "name": "Headless mode",
      "description": "Whether to run headless (defaults to true)",
      "envVar": "KURI_HEADLESS",
      "sensitive": false
    }
  ],
  "mcpServers": {
    "kuri": {
      "command": "node",
      "args": ["${extensionPath}/claude-code/mcp-server/dist/index.js"],
      "cwd": "${workspacePath}"
    }
  }
}
